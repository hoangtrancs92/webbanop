function toggleForm(){
    var container = document.querySelector('.container');
    container.classList.toggle('active');
    var container = document.querySelector('.section-header');
    container.classList.toggle('active-background');
}